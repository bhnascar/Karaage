<nav class="navbar navbar-inverse navbar-fixed-top navbar-translucent" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "Karaage", { controller: "home", action: "index"}, { class: "navbar-brand" }%>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><%= link_to "Blog", controller: "blog", action: "index" %></li>
          <li><%= link_to "Contact us", controller: "home", action: "contact" %></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <% if not currentUser %>
            <li><%= link_to "Log in", controller: "home", action: "login" %></li>
          <% else %>
            <p class="navbar-text"><%= image_tag currentUser.user_profile_url, class: "img-responsive nav-img-responsive" %></p>
            <p class="navbar-text">Welcome, <%= currentUser.user_first_name %></p>
            <li><%= link_to "Admin", controller: "home", action: "control_panel" %></li>
          <% end %>
        </ul>
      </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>